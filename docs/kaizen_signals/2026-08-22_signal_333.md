# Kaizen — valutazione delle 12 Regole, segnale #333

Generato automaticamente il 2026-08-22 14:08:45, stesso metodo di docs/KAIZEN_rule_application_worked_example.md (ogni risposta scritta e riletta da erpv6.kaizen.rule.answer, mai a memoria di conversazione).

**Segnale**: chiave `kg_coverage_missing_kaizen_button`, record `erpv6.kaizen.manual_report#0`, rilevato il 2026-08-22 14:04:39.

## Regola 0 — 0. Principio guida - Kaizen nativo, non un sistema a parte

Il segnale riguarda l'assenza di un'azione contestuale per erpv6.kaizen.manual_report. Per mantenere l'approccio Kaizen nativo, le segnalazioni devono collegarsi direttamente ai moduli esistenti (come crm.lead o project.task) tramite il pattern res_model (Char) e res_id (Integer) e la mail.thread del record interessato, senza creare DMS o interfacce isolate.

## Regola 1 — 1. Ogni problema e' un'opportunita'

I fatti mostrano 0 azioni con binding_model_id per erpv6.kaizen.manual_report. L'opportunita' consiste nell'aggiungere un'azione contestuale (ir.actions.server o ir.actions.act_window con binding_model_id e binding_view_types) e un wizard TransientModel che pre-compila res_model e res_id per permettere la creazione rapida di un report Kaizen direttamente dalla vista del record.

## Regola 2 — 2. Lavorare sul metodo, non colpevolizzare le persone

L'assenza dell'azione contestuale e' un limite di configurazione dell'interfaccia e del metodo, non una colpa degli utenti. L'integrazione del pulsante traccera' esclusivamente res_model e res_id del processo interessato, senza imputare la segnalazione alla responsabilita' di singoli dipendenti.

## Regola 3 — 3. Il miglioramento deve essere uno stile di vita

Non applicabile al caso specifico: il segnale riguarda la mancanza tecnica di un bottone contestuale (binding_model_id) nelle viste Odoo, non la gestione di promemoria ricorrenti o abitudini quotidiane tramite mail.activity o ir.cron.

## Regola 4 — 4. Basarsi su dati e fatti, non su opinioni

Verificato dal vivo ora (2026-08-22 14:08:11) con una query reale su ir.actions.act_window: 0 azioni con binding_model_id puntano a erpv6.kaizen.manual_report, su 3 azioni totali dei modelli erpv6.kaizen*. Confermato dal vivo: nessuna azione contestuale (binding_model_id) esiste ancora.

_Verificato dal vivo in questo giro._

## Regola 5 — 5. Non accettare lo status quo

Non applicabile al caso specifico: il segnale segnala un'azione mancante su un modello, non uno standard operativo obsoleto o prompt KB da rimettere in discussione con contatori di retry.

## Regola 6 — 6. Analizzare le cause profonde (5 Why)

Non applicabile al caso specifico: la mancanza dell'azione contestuale e' un problema chiaro di UI/configurazione ir.actions, per il quale non occorre avviare un processo di 5 Why con erpv6.kaizen.five_why.

## Regola 7 — 7. Coinvolgere tutti (cross-funzionalita')

Rendere disponibile l'azione contestuale su tutti i modelli permette a qualsiasi utente di qualsiasi reparto di segnalare un problema Kaizen. La gestione del coinvolgimento avviene tramite i permessi standard res.groups e notifica via mail.followers / message_post.

## Regola 8 — 8. Usare la creativita' per soluzioni a basso costo

Per classificare le segnalazioni provenienti dall'azione contestuale senza creare nuovi motori, si riusa erpv6.pareto.analysis.log_item per assegnare frequenza e impatto. Se il report riguarda aspetti CCP o fiscali, resta obbligatoria la validazione completa senza fast-track.

## Regola 9 — 9. Standardizzare i miglioramenti

Quando l'azione contestuale sara' presente e le segnalazioni Kaizen porteranno a un miglioramento confermato del processo, la nuova procedura standard verra' documentata in erpv6_library ed erpv6_typst con generazione del documento aggiornato.

## Regola 10 — 10. Eliminare gli sprechi (Muda)

L'assenza del pulsante contestuale causa uno spreco di movimento e tempo (Muda), poiche' costringe l'utente ad uscire dal contesto per registrare la segnalazione. L'aggiunta di binding_model_id elimina tale spreco e collega le segnalazioni a erpv6.pareto.item specificando il tipo di spreco.

## Regola 11 — 11. Rubrica di scoring - Pareto/Kairós/Heinrich su casi tecnici

Pareto: frequenza=5, impatto=3, punteggio=15. Kairos Tecnico: impatto_score=3, indicatori=[3,3,3,1,3 (Indicatore 5 = query reale sul grafo, 1 Fix precedente/i gia' collegato/i a questo modello)], prontezza_totale=13 (alta), quadrante=quick_win. Heinrich: lieve. Motivazione: Frequenza 5 (l'assenza dell'azione contestuale si verifica a ogni operazione di segnalazione dal contesto di un record) e impatto 3 (genera Muda di movimento e rallenta l'utente, ma esiste l'aggiramento tramite inserimento manuale nel modulo Kaizen). Kairos: causa verificata via query su ir.actions.act_window (ind1=3), fix additivo tramite definizione di binding_model_id/wizard (ind2=3), nessuna dipendenza esterna (ind3=3), nessun blocco di dati attivo (ind4=1). Heinrich: lieve poiche' genera una perdita di tempo reale ma non blocca ne' perde dati.

_Verificato dal vivo in questo giro._
