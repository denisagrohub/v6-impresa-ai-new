# Kaizen — valutazione delle 12 Regole, segnale #334

Generato automaticamente il 2026-08-22 14:09:31, stesso metodo di docs/KAIZEN_rule_application_worked_example.md (ogni risposta scritta e riletta da erpv6.kaizen.rule.answer, mai a memoria di conversazione).

**Segnale**: chiave `kg_coverage_typst_template_unmapped`, record `erpv6.typst.template#0`, rilevato il 2026-08-22 14:04:39.

## Regola 0 — 0. Principio guida - Kaizen nativo, non un sistema a parte

Il segnale riguarda l'aggregato dell'intera classe erpv6.typst.template (res_id=0). In linea con la Regola 0, il miglioramento va integrato nei flussi nativi mantenendo l'aggancio res_model='erpv6.typst.template' e res_id=0 tramite i motori esistenti (erpv6.kaizen.detected_signal / erpv6.heinrich.indicator.log_signal) senza creare UI o sottosistemi separati.

## Regola 1 — 1. Ogni problema e' un'opportunita'

Rilevati 7 record su 11 in erpv6.typst.template con typst_source vuoto/nullo e mancanti nel vocabolario Knowledge Graph. Il problema costituisce un'opportunita' per completare la mappatura del modello in docs/PLAN_knowledge_graph_phase1.md e valorizzare i templateTypst incompleti usando wizard o azioni contestuali gia' previste.

## Regola 2 — 2. Lavorare sul metodo, non colpevolizzare le persone

L'analisi si concentra sul processo e sul modello dati erpv6.typst.template (res_id=0), dove 7 template su 11 risultano privi di sorgente. Non si attribuiscono responsabilita me individuali; l'indicatore viene registrato tramite erpv6.heinrich.indicator.log_signal('erpv6.typst.template', 0, 'lieve', '7/11 template Typst con typst_source vuoto') per migliorare la qualita del sistema.

## Regola 3 — 3. Il miglioramento deve essere uno stile di vita

Si applica un promemoria/ricorrenza periodica tramite ir.cron su mail.activity.mixin per verificare la copertura dei modelli nel Knowledge Graph e rilevare tempestivamente nuovi template erpv6.typst.template privi di sorgente.

## Regola 4 — 4. Basarsi su dati e fatti, non su opinioni

Verificato dal vivo ora (2026-08-22 14:08:57) con una query reale su erpv6.typst.template: 11 record totali, 7 con typst_source vuoto/nullo. Il vocabolario del Knowledge Graph (docs/PLAN_knowledge_graph_phase1.md) non rappresenta ancora questo modello -- e' esattamente questo segnale a colmare quel buco (vedi Fix scritto a fine valutazione).

_Verificato dal vivo in questo giro._

## Regola 5 — 5. Non accettare lo status quo

La presenza di 7 record su 11 con typst_source nullo e la mancata inclusione di erpv6.typst.template nel vocabolario Knowledge Graph costituiscono uno status quo da superare. Si propone l'aggiornamento di docs/PLAN_knowledge_graph_phase1.md per includere questo modello.

## Regola 6 — 6. Analizzare le cause profonde (5 Why)

Per identificare la causa radice del perche 7 template su 11 siano stati creati senza codice sorgente typst_source, e' possibile applicare l'analisi dei 5 Why tramite un modello dedicato (es. erpv6.kaizen.five_why) per verificare se si tratta di placeholder non completati nella fase di setup.

## Regola 7 — 7. Coinvolgere tutti (cross-funzionalita')

La risoluzione della mappatura dei template Typst richiede il coinvolgimento cross-funzionale tra sviluppatori e responsabili documentali. Si utilizzano i canali nativi Odoo (mail.followers e message_post su erpv6.typst.template) e i gruppi di permessi res.groups esistenti per coordinare le attivita.

## Regola 8 — 8. Usare la creativita' per soluzioni a basso costo

La prioritizzazione della soluzione avviene inserendo l'elemento nel backlog tramite erpv6.pareto.analysis.log_item('erpv6.typst.template', 0, 'Mappatura KG e compilazione typst_source', 7, 3). Trattandosi di un adeguamento tecnico privo di impatti su CCP o fiscalita, la modifica puo seguire l'approvazione rapida una volta verificata nel backlog Pareto.

## Regola 9 — 9. Standardizzare i miglioramenti

Una volta definita la struttura standard dei template, il miglioramento viene standardizzato aggiornando la documentazione in docs/PLAN_knowledge_graph_phase1.md ed erpv6.library.document, garantendo che i documenti vengano generati correttamente tramite erpv6.typst.engine.generate_document.

## Regola 10 — 10. Eliminare gli sprechi (Muda)

Avere 7 template su 11 con typst_source vuoto e privi di mappatura KG rappresenta uno spreco di tipo Difetto/Processo (Muda). Il problema viene tracciato mediante erpv6.pareto.item per misurarne l'impatto ed eliminarlo, riusando i motori Pareto esistenti senza creare strutture duplicate.

## Regola 11 — 11. Rubrica di scoring - Pareto/Kairós/Heinrich su casi tecnici

Pareto: frequenza=3, impatto=3, punteggio=9. Kairos Tecnico: impatto_score=3, indicatori=[3,3,3,2,3 (Indicatore 5 = query reale sul grafo, 1 Fix precedente/i gia' collegato/i a questo modello)], prontezza_totale=14 (alta), quadrante=quick_win. Heinrich: lieve. Motivazione: Frequenza 3 e impatto 3 in quanto il problema riguarda 7 dei 11 record totali e l'assenza del modello dal vocabolario KG; causa radice confermata da query diretta (Ind.1 = 3), fix additivo di mappatura/sorgenti senza impatto su logica condivisa (Ind.2 = 3), nessuna dipendenza esterna (Ind.3 = 3) e impatto limitato ai record/modello interessati (Ind.4 = 2). Severita Heinrich lieve in quanto non vi e perdita o blocco irreversibile di dati.

_Verificato dal vivo in questo giro._
